#include "libft.h"

t_list	*ft_lstmap(t_list* *lst, void *(*f)(void *), void *(*del)(void *))
{
	t_list	*nodes;
	while (nodes)
	{
		if (???)// what does if NEEDED MEEEEEEAN????
			ft_lstdelone();
		else????
			ft_lstiter(nodes, del);
		
	}
}